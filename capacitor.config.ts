import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.feedingcare',
  appName: 'ionic_feedingCare_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
